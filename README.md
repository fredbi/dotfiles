# dotfiles

* tools:
  * nvim
  * ripgrep
  * tree-sitter,tree-sitter-cli
  * fd, fdfind
  * wezterm / kitty ?
  * luarocks
  * fzf
  * mmdc (mermaid rendering) (via npm)
  * lazygit (manual)

Being installed:

Not installed yet:
  * ast-grep ?


lazyvim plugins
